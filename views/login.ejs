<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= titulo %></title>
    <!-- Tailwind -->
    <link href="/css/output.css" rel="stylesheet">
    <script src="/js/login.js" defer></script>
</head>
<body class="bg-zinc-900 text-white flex items-center justify-center min-h-screen">
    <div class="w-full max-w-md p-8 space-y-6 bg-zinc-950 rounded-lg shadow-lg border border-zinc-800">
        <h1 class="text-2xl font-bold text-center">Iniciar Sesión</h1>
        <form id="loginForm" action="/login" method="POST" class="space-y-6" novalidate>
            <div>
                <label for="nombre" class="text-sm font-medium">Nombre de Usuario</label>
                <input type="text" name="nombre" id="nombre" required class="w-full px-3 py-2 mt-1 text-white bg-zinc-800 border border-zinc-700 rounded-md focus:outline-none focus:ring-2 focus:ring-emerald-500">
            </div>
            <div>
                <label for="password" class="text-sm font-medium">Contraseña</label>
                <input type="password" name="password" id="password" required class="w-full px-3 py-2 mt-1 text-white bg-zinc-800 border border-zinc-700 rounded-md focus:outline-none focus:ring-2 focus:ring-emerald-500">
            </div>
            <!-- Contenedor para errores (funciona con y sin JS) -->
            <p id="errorContainer" class="text-sm text-rose-500 <%= (typeof error === 'undefined' || !error) ? 'hidden' : '' %>"><%= typeof error !== 'undefined' ? error : '' %></p>
            <div>
                <button type="submit" class="w-full flex justify-center items-center px-4 py-2 font-semibold text-white bg-emerald-600 rounded-md hover:bg-emerald-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-emerald-500 focus:ring-offset-zinc-900 disabled:opacity-50 disabled:cursor-not-allowed">
                    Entrar
                </button>
            </div>
        </form>
    </div>
</body>
</html>
